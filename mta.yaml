_schema-version: 3.3.0
ID: customerdetails
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: customerdetails-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: customerdetails-auth
      - name: customerdetails-db
      - name: customerdetails-destination

  - name: customerdetails-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: customerdetails-db

  - name: customerdetails-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: customerdetails-auth
      - name: srv-api
      - name: customerdetails-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: customerdetailscustomerdetailsui
          artifacts:
            - customerdetailsui.zip
          target-path: app/

  - name: customerdetailscustomerdetailsui
    type: html5
    path: app/customerdetailsui
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: customerdetails-destinations
    type: com.sap.application.content
    requires:
      - name: customerdetails-auth
        parameters:
          service-key:
            name: customerdetails-auth-key
      - name: customerdetails-html5-repo-host
        parameters:
          service-key:
            name: customerdetails-html5-repo-host-key
      - name: srv-api
      - name: customerdetails-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: customerdetails-html5-repository
              ServiceInstanceName: customerdetails-html5-repo-host
              ServiceKeyName: customerdetails-html5-repo-host-key
              sap.cloud.service: customerdetails.service
            - Name: customerdetails-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: customerdetails-auth
              ServiceKeyName: customerdetails-auth-key
              sap.cloud.service: customerdetails.service

resources:
  - name: customerdetails-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: customerdetails-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'customerAdmin (customerdetails ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.customerAdmin'
          - name: 'customerDisplay (customerdetails ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.customerDisplay'
  - name: customerdetails-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared

  - name: customerdetails-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: customerdetails-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
  - name: customerdetails-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
